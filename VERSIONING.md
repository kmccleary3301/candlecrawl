# Versioning Policy

CandleCrawl follows Semantic Versioning (`MAJOR.MINOR.PATCH`).

- MAJOR: breaking API contract changes.
- MINOR: backwards-compatible feature additions.
- PATCH: backwards-compatible fixes and non-contract changes.

Contract artifacts are versioned with each release tag.
